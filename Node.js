export default class Node {
    constructor(leftNode, rightNode, key, value) {
        this.leftNode = leftNode
        this.rightNode = rightNode
        this.key = key
        this.value = value
    }
}
